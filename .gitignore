# build output
dist/
.astro/

# dependencies
node_modules/

# editors / os
.DS_Store
Thumbs.db
*.log

# env
.env
.env.production

# NOTE: public/icons and public/favicon.svg are generated by
# `npm run icons`, but they are committed on purpose — `npm run dev`
# does not run the generator, and the app should work on a fresh clone.
